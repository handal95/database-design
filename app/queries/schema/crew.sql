crew_code VARCHAR(16) PRIMARY KEY,

crew_name VARCHAR(32) NOT NULL,
nationality VARCHAR(64) NULL,
crew_birth_date DATE NULL,
crew_name_eng VARCHAR(32) NULL,
sex VARCHAR(16) NULL,
profile_image BLOB NULL
